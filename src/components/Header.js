import React from 'react';

const Header = (props) => (
  <div className="header">
    <div className="container">
      <h1 className="header__title">Random value Picker</h1>
      <h2 className="header__subtitle">
        Create list to randomly pick one of the items
      </h2>
    </div>
  </div>
);

export default Header;
